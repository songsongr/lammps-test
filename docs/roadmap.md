# 未来优化路线图 (2026-09-23 刷新)

> 本文档为项目内路线图; 已落地能力详见 ARCHITECTURE.md 与 docs/work-log.md (v1.5–v1.8)。

## 已落地 (2026-08-28 → 09-05)

lint 发车拦截 · 失败案例库 (外置 JSON) · 一致性链 · 夹具回归 · CLI 任务入库 · 任务队列+并发上限 ·
检查点续跑 · 参数扫描+批次对比 · 弛豫双轴图 · 3D 初末态对比 · 本地工具 (VMD/Vesta 探测+启动器) ·
浏览器 3D 预览 (3Dmol) · AI 桥① (复制分析提示词) · AI 桥② (skills/lammps-diagnose) ·
受控退出 · 手动创建指南 · 兜底注册 · systems/ 布局重排 ·
迁移暂存区 `_inbox` 全套 (列表/确认入库/丢弃 API + 指南抽屉待入库清单) ·
单任务溯源包导出 (`GET /api/jobs/{id}/export` + 任务参数卡按钮) ·
MSD 末窗扩散系数拟合 (`fit_diffusion` + 轨迹统计卡展示) ·
跨任务全原子 MSD 对比 (真实滞后步数对齐, 2026-09-23) ·
蒙脱石 4260K 根因修复 (cif_binding oh 阈值 + 羟基 O-H 键 + 正规动力学) ·
v1.8.5 runner 互递归 / CLI ws_id 错位 两处遗留 bug 修复

## 仍开放 (按价值排序)

1. **体系来源扩展** — 晶体 profile (金属/陶瓷, 沿用 clay_cif 思路) / 摩擦 rig+剪切模板 /
   导入 .data 路径 (打通生物大分子)。每个都是独立特性。
2. **力场文献值替换** — Sr/Cl LJ 参数 (Aqvist/Mamatkulov 候选), 改 system.json → 重建 → 重跑;
   属科学决策, 需拍板。
3. **对比图表增强 (余项)** — 扫描批次按参数归一 x 轴 (现按行号对齐) /
   MSD 分原子类型对比 (当前跨任务对比只绘全原子曲线)。
4. **失败案例库 UI 编辑器** — 现为文件策展 (failure_rules.json), 可加工作台编辑界面。
5. **CodeMirror 在线编辑器** — 脚本编辑语法高亮 (现为纯文本 textarea)。
6. **平衡判据扩展** — 多段模板每段独立判定 / NPT 专用判据。
7. **蒙脱石生产加长** — 4260K 修复后重跑纳秒级生产, 获取更收敛的 D (需用户拍板算力预算)。

## 不再追

- LLM 自律式门禁 (确定性机制已全覆盖; VASP Harness 翻车实录为证)
- 嵌入式重型可视化 (3Dmol 预览 + VMD/Vesta 启动器已覆盖)
- pyiron/AiiDA 本体引入 (思想已吸收: 记账式任务/溯源指纹 → 溯源包已落地)
